class Member:
	def __init__(self, name, age):
		self.id = id
		self.name = name
		self.age = age

class Post:
	def __init__(self, title, body):
		self.id = id
		self.title = title
		self.body = body

